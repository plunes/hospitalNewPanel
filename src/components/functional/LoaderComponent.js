import React from "react"

const LoaderComponent = (props) => {

    return(
        <div style={{...props.style}} className="loader_ris loader-1_ris">
        </div>
    )
}

export default LoaderComponent